#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers.
 *
 * @a: The input number as an integer
 * @b: The input number as an integer
 *
 * Return: the resutl
 */
int mul(int a, int b)
{
	return (a * b);
}
