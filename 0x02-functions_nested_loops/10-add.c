#include "main.h"

/**
 * add - Print the add of two numbers
 *
 * @a: input number as an integer
 * @b: input number as an integer
 *
 * Return: number
 */
int add(int a, int b)
{
	return (a + b);
}
