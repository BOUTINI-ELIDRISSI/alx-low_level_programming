#include "main.h"
#include <string.h>

/**
 * _strlen - a function that returns the length of a string.
 *
 * @s: input
 *
 * Return: lenght
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
